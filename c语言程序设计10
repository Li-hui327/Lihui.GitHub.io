#include <stdio.h>
float factorial(float x)
{	  float sum;
	  for(sum=1;x>1;x--)
	  {   sum=sum*x;
	  }
	  return sum;
}
int main()
{	float n,e=1;
	for(n=1;(1/factorial(n))>1E-6;n++)
	{	e+=1/factorial(n);
	}
	printf("e=%f",e);
	return 0;
}
